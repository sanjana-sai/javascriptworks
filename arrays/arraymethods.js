//array methods

var colors=["red","green","blue"]

console.log(colors);




//class Array{

     //push(object){}  adding a element last in a array
     
     //pop()         to remove  the element  in array and it returns the value   eithe we can store the value to another variable
     
     //unshift(object)    adding the element first to an array

     //shift             removing the first element in a array

     //includes(object)   to check whether the object is in array and returns true or false

//}


colors.push("yellow")
console.log(colors);


var poped_obj=colors.pop()

console.log(colors);

console.log(colors.includes("black"))


